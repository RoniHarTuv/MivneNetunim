public class sortsTirgul {
    /**
     * sorts and runTime complexity:
     *
     *1. Quick sort:
     *   Best case O(n log(n))
     *   Worst case O(n^2) if already sorted
     *
     *2. Merge sort
     *   O(n Log n)
     *
     *3. Selection sort:
     *   O(n^2)
     *   good for small data.
     *
     *4. Insertion sort:
     *   O(n^2)
     *   good for small data.
     *
     *5. Bubble sort
     *   O(n^2)
     *   very bad for big data.
     */
}
